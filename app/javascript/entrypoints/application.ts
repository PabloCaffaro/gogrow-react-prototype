/**
 * Entrypoint de ejemplo creado por Vite Ruby.
 * El layout actual inicia React desde `inertia.tsx`; este archivo se conserva
 * como referencia si más adelante se necesita una página Vite sin Inertia.
 */
console.log('Vite ⚡️ Rails')

console.log('Visit the guide for more information: ', 'https://vite-ruby.netlify.app/guide/rails')

// Ejemplos inactivos de integraciones Rails que podrían cargarse desde Vite.
// import * as Turbo from '@hotwired/turbo'
// Turbo.start()
//
// import ActiveStorage from '@rails/activestorage'
// ActiveStorage.start()
//
// Importa todos los canales Action Cable si el producto incorpora tiempo real.
// const channels = import.meta.glob('./**/*_channel.js', { eager: true })

// Ejemplo de carga de una hoja de estilos adicional desde este entrypoint.
// import '~/index.css'
